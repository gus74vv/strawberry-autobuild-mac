name: Build Strawberry macOS ARM64

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-macos-arm64:
    runs-on: macos-latest
    steps:
      - name: Checkout autobuild repo
        uses: actions/checkout@v4

      - name: Descargar Strawberry
        run: git clone https://github.com/strawberrymusicplayer/strawberry.git src

      - name: Instalar dependencias
        run: |
          brew update
          brew install cmake ninja qt@6 pkg-config

      - name: Configurar CMake (ARM64)
        run: |
          cmake -S src -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_OSX_ARCHITECTURES=arm64 \
            -DCMAKE_PREFIX_PATH="$(brew --prefix qt@6)/lib/cmake"

      - name: Compilar
        run: cmake --build build --config Release

      - name: Subir artefacto
        uses: actions/upload-artifact@v4
        with:
          name: strawberry-macos-arm64
          path: build/bin/strawberry
